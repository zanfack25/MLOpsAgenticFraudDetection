name: Build_and_Deploy_AgentAI_Containers_to_EKS

on:
  push:
    branches: [main]

env:
  MODEL_BUCKET: dav-fraud-detection-models
  AWS_REGION: ca-central-1

jobs:
  jobs:
  build-agent1:
    uses: ./.github/workflows/agent1-workflow.yml
    with:
      image_tag: latest

  build-agent2:
    uses: ./.github/workflows/agent2-workflow.yml
    with:
      image_tag: latest

  build-agent3:
    uses: ./.github/workflows/agent3-workflow.yml
    with:
      image_tag: latest